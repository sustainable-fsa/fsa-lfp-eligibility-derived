.Rproj.user
.Rhistory
.RData
.Ruserdata
.Rdata
.httr-oauth
.DS_Store
.quarto

# S3-hosted data (archive of record:
# https://data.sustainable-fsa.com/fsa-lfp-eligibility-derived/).
# The derived records are mirrored in git as CSV and Parquet; the weekly USDM
# inputs it is built from are far larger and live on S3 only.
data
usdm.parquet
manifest.json
manifest.html
